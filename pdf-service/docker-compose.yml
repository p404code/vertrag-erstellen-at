services:
  vertrag-pdf-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertrag-pdf-service
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      - PORT=3000

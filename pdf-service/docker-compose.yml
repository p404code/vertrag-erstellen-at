services:
  vertrag-pdf-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: vertrag-pdf-service:latest
    container_name: vertrag-pdf-service
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      - PORT=3000
